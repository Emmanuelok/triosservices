# Keep actual values in Vercel environment settings. Never commit secrets.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
SUPABASE_SERVICE_ROLE_KEY=
# Use the dedicated Trios project's pooled PostgreSQL URL, with SSL enabled.
DATABASE_URL=
# Only verified accounts matching this server-controlled list become admins.
OWNER_EMAILS=
SITE_ORIGIN=
# Optional; guided care planning works without this.
OPENAI_API_KEY=
